// OBSOLETO: este cliente axios não é mais usado. O app agora é 100% client-side
// e usa `src/store/localStore.ts` (localStorage) em vez de chamadas HTTP a um backend.
// Mantido apenas até a próxima limpeza manual do repositório; nenhum arquivo importa este módulo.
export {};
